# =====================================================================
# Stage 3: 文献单倍群年龄与本项目 ρ/ML 年龄比较配置文件
# 所有相对路径均相对于 project.base_dir
# =====================================================================

project:
  base_dir: "/mnt/f/OneDrive/文档（科研）/脚本/Download/10-mtDNA/12-单倍群年龄估计"  # 项目根目录

# --------------------------------------------------------------------- #
# 输入、输出与日志目录                                                   #
# --------------------------------------------------------------------- #
paths:
  python_dir: "python"                                      # Python 模块目录
  download_dir: "download"                                  # 文献年龄表目录
  project_comparison: "output/stage_2/results/ml_vs_rho_comparison.tsv"  # 本项目 ρ/ML 比较表
  results_dir: "output/stage_3_compare/results"             # Stage 3 结果目录
  log_dir: "output/stage_3_compare/log"                     # Stage 3 日志目录
  report_dir: "report"                                      # 中文报告目录

# --------------------------------------------------------------------- #
# 工具路径                                                               #
# --------------------------------------------------------------------- #
tools:
  conda_bin: "/home/luolintao/miniconda3/bin/conda"         # conda 可执行文件
  python_env_prefix: "/home/luolintao/miniconda3/envs/BigLin"  # Python 环境前缀

# --------------------------------------------------------------------- #
# 比较参数                                                               #
# --------------------------------------------------------------------- #
runtime:
  large_diff_kya: "5"                                       # 仅作差异参考；主判定采用上下限是否覆盖
  large_rel_diff_pct: "30"                                  # 仅作差异参考；主判定采用上下限是否覆盖
